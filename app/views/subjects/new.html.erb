 <h1>Create a Subject</h1>
  <%= form_for [@topic, @subject] do |f| %>
  <%= f.label :Subject %>
  <%= f.text_field :name, :placeholder => ("Enter a subject")%>

  <%= f.submit "Submit", class: "btn btn-primary" %>

  <%end%>
